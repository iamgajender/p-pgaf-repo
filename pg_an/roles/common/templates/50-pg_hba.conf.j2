# ----------------------------------------------------
# Generated by PostgreSQL Automation Portal
# DO NOT EDIT MANUALLY
# ----------------------------------------------------

# Local connections
local   all             postgres                                peer
local   all             all                                     peer

# IPv4 localhost
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 localhost
host    all             all             ::1/128                 scram-sha-256

{% for item in pg_hba_entries %}
host    {{ item.database }}    {{ item.user }}    {{ item.address }}    {{ item.method }}
{% endfor %}
